/**
 * DTO barrel — re-exports everything from ApiResponseDto for convenient
 * single-import usage in routes:
 *
 *   import { successResponse, errorResponse, paginatedResponse } from '../dto/index.js';
 */

export * from "./ApiResponseDto.js";
